name: UI Tests

on:
  schedule:
    - cron: '0 22 * * 0'  # Sunday midnight Vienna (UTC+2)
    - cron: '0 22 * * 3'  # Wednesday midnight Vienna (UTC+2)
  workflow_dispatch:

jobs:
  test-ui:
    name: UI Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: UI tests coming soon
        run: echo "UI tests coming soon"
