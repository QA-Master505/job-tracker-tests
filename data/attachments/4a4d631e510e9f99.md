# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ui/auth/register.spec.ts >> Registration >> should register a new user successfully
- Location: tests/ui/auth/register.spec.ts:22:7

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/login/
Received string:  "https://job-tracker-frontend-green-sigma.vercel.app/register"
Timeout: 10000ms

Call log:
  - Expect "toHaveURL" with timeout 10000ms
    24 × unexpected value "https://job-tracker-frontend-green-sigma.vercel.app/register"

```

```yaml
- navigation:
  - link "JobTracker":
    - /url: /
  - link "Login":
    - /url: /login
  - link "Register":
    - /url: /register
- main:
  - heading "Create Account" [level=2]
  - paragraph: Registration failed. Please try again.
  - text: Username
  - textbox: test_1783928657088
  - text: Email
  - textbox: test-1783928657088@example.com
  - text: Password
  - textbox: TestPlaywright1
  - button "Register"
  - paragraph:
    - text: Already have an account?
    - link "Log In":
      - /url: /login
- contentinfo: © 2026 JobTracker. All rights reserved.
```

# Test source

```ts
  1  | import { Page, expect } from '@playwright/test';
  2  | 
  3  | export class RegisterPage {
  4  |   constructor(private page: Page) {}
  5  | 
  6  |   async goto() {
  7  |     const base = process.env.BASE_URL || 'http://localhost:5173';
  8  |     await this.page.goto(`${base}/register`);
  9  |   }
  10 | 
  11 |   // Inputs have no name/id — ordered as: username (text), email, password
  12 |   async register(username: string, email: string, password: string) {
  13 |     await this.page.fill('input[type="text"]', username);
  14 |     await this.page.fill('input[type="email"]', email);
  15 |     await this.page.fill('input[type="password"]', password);
  16 |     await this.page.click('button[type="submit"]');
  17 |   }
  18 | 
  19 |   // Error is a <p> with class bg-red-50 (no role="alert" or data-testid)
  20 |   async getErrorMessage() {
  21 |     const el = this.page.locator('p.bg-red-50');
  22 |     await el.waitFor({ timeout: 5000 }).catch(() => {});
  23 |     return await el.textContent().catch(() => null);
  24 |   }
  25 | 
  26 |   async expectRegisterPageVisible() {
  27 |     await expect(this.page).toHaveURL(/\/register/);
  28 |   }
  29 | 
  30 |   // Successful registration redirects to /login
  31 |   async expectSuccessRedirect() {
> 32 |     await expect(this.page).toHaveURL(/\/login/, { timeout: 10000 });
     |                             ^ Error: expect(page).toHaveURL(expected) failed
  33 |   }
  34 | }
  35 | 
```